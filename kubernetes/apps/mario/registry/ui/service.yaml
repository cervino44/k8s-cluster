apiVersion: v1
kind: Service
metadata:
  name: registry-ui
spec:
  type: ClusterIP
  selector:
    run: registry-ui
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  
  