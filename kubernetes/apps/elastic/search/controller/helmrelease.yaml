apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: elastic-operator
spec:
  chart:
    spec:
      chart: elastic
      interval: 30m
      sourceRef:
        kind: HelmRepository
        name: eck-operator
        namespace: flux-system
  interval: 30m
